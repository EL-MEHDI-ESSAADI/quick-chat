type Message @model {
  name: String!
  avatar: URL
  body: String!
  likes: Int @default(value: 0)
}

schema @auth(providers: [{ type: jwt, issuer: "nextauth", secret: "{{ env.NEXTAUTH_SECRET }}" }], rules: [{ allow: private }]) {
  query: Query
}
