export type User = {
  id: string;
  name: string;
  email: string;
  avatar: string;
  username: string;
  collectionId: string;
  collectionName: string;
  emailVisibility: boolean;
  created: string;
  updated: string;
  verified: boolean;
} | null;
